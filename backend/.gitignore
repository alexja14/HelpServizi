node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# SQLite runtime artifacts (do not commit local data / temp / backups)
data.sqlite
*.sqlite
.*.sqlite.tmp
db_backups/
