## Backend server
PORT=4000
NODE_ENV=production

## Admin basic auth credentials (change these!)
ADMIN_USER=admin
ADMIN_PASS=changeme

## SQLite persistence (relative paths are resolved from backend/dist at runtime)
# Optional: override where the sqlite file is stored
# SQLITE_PATH=./data.sqlite

## Persistence + backup tuning (defaults shown)
# DB_PERSIST_DEBOUNCE_MS=300
# DB_BACKUP_INTERVAL_MS=300000
# DB_BACKUPS_TO_KEEP=10
# DB_BACKUP_DIR=./db_backups

## (If you re-enable Prisma usage)
# DATABASE_URL="file:./dev.db"

## Frontend build environment examples (Vite uses VITE_ prefix to expose)
# VITE_API_BASE=""

## Copy this file to .env and adjust values for production.